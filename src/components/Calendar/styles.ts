import styled from 'styled-components';

export const TableStyle = styled.div`
  && {
    .rsvp-horizontal {
      margin: 0 auto;
      overflow: auto;

      .th-col {
        font-size: 12px;
        text-align: center;
      }

      td {
        font-size: 12px;
        text-align: center;
      }
    }
  }
`;