import React from 'react';

import { Layout } from 'antd';

const { Header } = Layout;

const Navigation = () => {
  return (
    <Header className="App-header">
      <p>Header</p>
    </Header>
  );
};

export default Navigation;
