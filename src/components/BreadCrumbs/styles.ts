import styled  from 'styled-components';

export const StyledDiv = styled.div`
  && {
    margin-bottom: 15px;
    .ant-breadcrumb-link {
      display: flex;
      justify-content: flex-start;
      align-items: center;
    }
  }
`;